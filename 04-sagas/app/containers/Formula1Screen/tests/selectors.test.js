// import { fromJS } from 'immutable';
// import { makeSelectFormula1ScreenDomain } from '../selectors';

// const selector = makeSelectFormula1ScreenDomain();

describe('makeSelectFormula1ScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
